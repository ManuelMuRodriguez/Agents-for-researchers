services:
  latex:
    image: texlive/texlive:latest
    working_dir: /paper
    volumes:
      - .:/paper
    command: >
      sh -c "latexmk -pdf -interaction=nonstopmode -file-line-error main.tex
             && echo '✅ PDF generado: main.pdf'"
